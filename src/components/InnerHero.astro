---
interface Props { eyebrow: string; title: string; lead: string; }
const { eyebrow, title, lead } = Astro.props;
---
<section class="inner-hero"><div class="container narrow"><p class="eyebrow">{eyebrow}</p><h1>{title}</h1><p class="inner-lead">{lead}</p></div></section>
